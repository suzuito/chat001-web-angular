import { AgentNameDirective } from './agent-name.directive';

describe('AgentNameDirective', () => {
  it('should create an instance', () => {
    const directive = new AgentNameDirective();
    expect(directive).toBeTruthy();
  });
});
