import { AgentDescriptionDirective } from './agent-description.directive';

describe('AgentDescriptionDirective', () => {
  it('should create an instance', () => {
    // const directive = new AgentDescriptionDirective();
    // expect(directive).toBeTruthy();
  });
});
